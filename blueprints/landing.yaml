title: Landing
'@extends': default

form:
  fields:
    tabs:
      fields:
        content:
          fields:

            header.selected_media:
              label: Media
              type: pagemediaselect
              
            header.selected_media_alt:
              type: text
              label: Media Alt

        advanced:
          fields:
            columns:
              fields:
                column1:
                  fields:
                    header.body_classes:
                      markdown: true
                      description: '```small_title```'